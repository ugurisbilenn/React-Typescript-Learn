export type instructorProps={
    instNameLastName :{
        firstName:string;
        lastName:string;
    }
};

export type DataComing = {
    name:string;
    courseNumber:number;
    isBest:boolean;
};
